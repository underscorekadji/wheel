# Application Environment
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Redis Configuration
# Option 1: Use Redis URL (preferred for production)
REDIS_URL=redis://localhost:6379

# Option 2: Use individual Redis settings (alternative to REDIS_URL)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Socket.IO Configuration (for future use)
# SOCKET_IO_PORT=3001